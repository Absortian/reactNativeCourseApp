import { StyleSheet } from "react-native";

export const Styles = StyleSheet.create({
    image: {
        width: "100%",
        height: 200,
        resizeMode: "contain",
        marginTop: 20
    },
    content: {
        marginTop: 20,
        marginHorizontal: 20
    }
});